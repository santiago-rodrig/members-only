<h1>Please Login</h1>

<%= form_with(scope: :session, url: login_url, local: true) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit %>
<% end %>
